/* eslint-disable no-unexpected-multiline */
const otherName = 'Freddy'
typeof otherName === 'string'

console.log('test')

// if (!'servicework' in navigator) {
//   // old browsers
// }

const greeting = 'hello'
console
  .log(`${greeting} world!`)

  [(1, 2, 3)].forEach(element => {
    console.log(element)
  })
